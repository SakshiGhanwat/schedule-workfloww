name: Schedule Job

on: 
  schedule:
    - cron: "0 0 * * *" # Every day at midnight

jobs: 
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "done"

       
  
    
        
